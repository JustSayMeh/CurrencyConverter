<UserControl
    x:Class="CurrencyConverter.Converter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CurrencyConverter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
        <controls:DockPanel>
            <TextBox Name="ValueA" Height="auto" Margin="10,0,10,0" Width="150" controls:DockPanel.Dock="Left"/>
            <TextBox Name="ValueB" Height="auto" Margin="10,0,10,0" Width="150" controls:DockPanel.Dock="Right"/>
            <Button Content="Swap"/>
        </controls:DockPanel>
        <StackPanel Name="ConvertButton" Height="auto" Margin="0,10,0,0" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Button" controls:DockPanel.Dock="Bottom"/>
        </StackPanel>
    </StackPanel>
</UserControl>
